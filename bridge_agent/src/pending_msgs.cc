#include <pending_msgs.h>
#include <pending_msg_reg.h>

//template void pending_msg_registry<APM>::add_pending_message( std::string, pending_message_t&& );
//template 
//std::list<init_pending_msg> pending_msg_registry<APM>::_get_all_msgs_by_type<init_pending_msg>( bool );
